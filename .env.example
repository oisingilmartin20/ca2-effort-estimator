# Copy to .env and fill in. If OPENAI_API_KEY is unset the app falls back
# to a deterministic heuristic estimator so the UI is fully demoable offline.
OPENAI_API_KEY=<YOUR_OPENAI_API_KEY>
ESTIMATOR_MODEL=llama-3.3-70b-versatile
OPENAI_BASE_URL=https://api.groq.com/openai/v1
